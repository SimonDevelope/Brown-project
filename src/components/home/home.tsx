import React from 'react';
import Header from '../header/header';

import './home.scss';

const home: React.FC = () => {
  return (
    <div className="home-total-view-port">
      <Header />
    </div>
  );
};

export default home;
