import React from 'react';
import Calendar from './calendar/calendar';
import './main.scss';

const main = () => {
  return (
    <div className="main-total-view-port">
      <Calendar />
    </div>
  );
};

export default main;
